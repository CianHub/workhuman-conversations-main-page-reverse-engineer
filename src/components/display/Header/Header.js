import React from 'react';
import PropTypes from 'prop-types';

import './Header.css';

const Header = (props) => {
  return <header className="app-content-header">Header</header>;
};

Header.propTypes = {};

export default Header;
